-- ============================================
-- GUI تختفي بعد 3 ثواني (نسخة War Tycoon)
-- ============================================

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local PlayerGui = LP:WaitForChild("PlayerGui")

local gui = Instance.new("ScreenGui")
gui.Name = "MessageGUI"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = PlayerGui

-- الإطار الرئيسي
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 550, 0, 200)
frame.Position = UDim2.new(0.5, -275, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(10, 10, 25)
frame.BackgroundTransparency = 0.05
frame.BorderSizePixel = 0
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 20)
corner.Parent = frame

-- حدود قوس قزح
spawn(function()
    while gui and gui.Parent do
        local hue = tick() % 2 / 2
        frame.BorderColor3 = Color3.fromHSV(hue, 1, 1)
        frame.BorderSizePixel = 3
        wait(0.05)
    end
end)

-- النص الرئيسي
local mainText = Instance.new("TextLabel")
mainText.Size = UDim2.new(1, -40, 0, 60)
mainText.Position = UDim2.new(0, 20, 0, 20)
mainText.Text = "اهلا كافي تلعب وتسخت 🤍"
mainText.TextScaled = true
mainText.TextColor3 = Color3.fromRGB(0, 200, 255)
mainText.BackgroundTransparency = 1
mainText.Font = Enum.Font.GothamBold
mainText.Parent = frame

-- النص الثاني
local subText = Instance.new("TextLabel")
subText.Size = UDim2.new(1, -40, 0, 50)
subText.Position = UDim2.new(0, 20, 0, 85)
subText.Text = "ادخل ماب War Tycoon وجمع"
subText.TextScaled = true
subText.TextColor3 = Color3.fromRGB(255, 200, 100)
subText.BackgroundTransparency = 1
subText.Font = Enum.Font.Gotham
subText.Parent = frame

-- النص الثالث
local thirdText = Instance.new("TextLabel")
thirdText.Size = UDim2.new(1, -40, 0, 50)
thirdText.Position = UDim2.new(0, 20, 0, 135)
thirdText.Text = "ترا بكرا بحدد من يصير مساعدي 🖤"
thirdText.TextScaled = true
thirdText.TextColor3 = Color3.fromRGB(150, 255, 150)
thirdText.BackgroundTransparency = 1
thirdText.Font = Enum.Font.GothamBold
thirdText.Parent = frame

-- النص الرابع
local lastText = Instance.new("TextLabel")
lastText.Size = UDim2.new(1, -40, 0, 40)
lastText.Position = UDim2.new(0, 20, 0, 178)
lastText.Text = "ومحمد صار قوي 🔥"
lastText.TextScaled = true
lastText.TextColor3 = Color3.fromRGB(255, 150, 0)
lastText.BackgroundTransparency = 1
lastText.Font = Enum.Font.GothamBold
lastText.Parent = frame

print("✅ الرسالة تظهر 3 ثواني ثم تختفي")

-- تختفي بعد 3 ثواني
task.wait(8)
gui:Destroy()
